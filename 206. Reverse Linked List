lass Solution:
    def reverseList(self, head: ListNode) -> ListNode:
        
        previous = None
        node = head
        
        while node:
            next = node.next
            node.next = previous
            previous = node
            node = next
            
            #node = next
        return previous
            
        
